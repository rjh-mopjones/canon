[package]
name = "canon-inbox"
version = "0.1.0"
edition = "2021"

[dependencies]
canon-core = { path = "../canon-core" }
async-trait = "0.1"
thiserror = "2"
uuid = { version = "1", features = ["v4"] }
