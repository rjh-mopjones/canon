apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# GKE overlay — placeholder for production deployment.
#
# When ready, this overlay should:
# - Set image refs to Artifact Registry (e.g. gcr.io/canon-project/fleet-service:latest)
# - Add Ingress for gateway + frontend
# - Increase replicas for services and gateway
# - Add HorizontalPodAutoscaler resources
# - Adjust resource limits for production workloads
# - Switch Cassandra replication to NetworkTopologyStrategy
# - Consider managed alternatives (Cloud SQL, Astra, Confluent Cloud)

resources:
  - ../../base
