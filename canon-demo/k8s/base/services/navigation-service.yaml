apiVersion: apps/v1
kind: Deployment
metadata:
  name: navigation-service
  labels:
    app: navigation-service
    app.kubernetes.io/name: navigation-service
    app.kubernetes.io/part-of: canon-demo
    tier: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: navigation-service
  template:
    metadata:
      labels:
        app: navigation-service
        tier: app
    spec:
      initContainers:
        - name: wait-infra
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for YugabyteDB..."
              until nc -z yugabytedb 5433; do sleep 2; done
              echo "Waiting for Cassandra..."
              until nc -z cassandra 9042; do sleep 2; done
              echo "Waiting for Kafka..."
              until nc -z kafka 9092; do sleep 2; done
              echo "All infrastructure ready."
      containers:
        - name: navigation-service
          image: canon-demo/navigation-service
          env:
            - name: YUGABYTE_URL
              valueFrom:
                configMapKeyRef:
                  name: canon-config
                  key: yugabyte_url
            - name: CASSANDRA_NODES
              valueFrom:
                configMapKeyRef:
                  name: canon-config
                  key: cassandra_nodes
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: canon-config
                  key: kafka_brokers
            - name: RUST_LOG
              value: info
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
