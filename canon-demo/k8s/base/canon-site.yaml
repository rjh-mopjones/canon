apiVersion: apps/v1
kind: Deployment
metadata:
  name: canon-site
  labels:
    app: canon-site
    app.kubernetes.io/name: canon-site
    app.kubernetes.io/part-of: canon-demo
    tier: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: canon-site
  template:
    metadata:
      labels:
        app: canon-site
        tier: app
    spec:
      containers:
        - name: canon-site
          image: canon-demo/canon-site
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: canon-site
  labels:
    app: canon-site
    app.kubernetes.io/name: canon-site
    app.kubernetes.io/part-of: canon-demo
    tier: app
spec:
  type: LoadBalancer
  selector:
    app: canon-site
  ports:
    - port: 80
      targetPort: 80
