[toolchain]
channel = "stable"
components = ["clippy", "rustfmt"]
# aarch64-unknown-linux-musl: cross-compilation for k8s deployment (make k8s-build)
# wasm32-unknown-unknown: frontend WASM via trunk
targets = ["aarch64-unknown-linux-musl", "wasm32-unknown-unknown"]
